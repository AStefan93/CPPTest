{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Release",
            "command": "bazel build --config clang_release ... && bazel run @hedron_compile_commands//:refresh_all -- --config=clang_release && bazel build --config=iwyu ...",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "type": "shell",
            "label": "Build Debug",
            "command": "bazel build --config clang_debug ... && bazel run @hedron_compile_commands//:refresh_all -- --config=clang_debug && bazel build --config=iwyu ...",
            "group": "build"
        },
        {
            "type": "process",
            "label": "Clean",
            "command": "bazel",
            "args": [
                "clean"
            ],
            "group": "build"
        }
    ],
    "version": "2.0.0"
}